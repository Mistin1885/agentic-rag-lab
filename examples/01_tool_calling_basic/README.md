# 01 Tool Calling Basic

Goal: build the smallest possible tool-calling loop.

This exercise is intentionally not implemented yet. It should eventually demonstrate:

- One user request
- One model tool call
- One local Python function execution
- One final answer using the tool result

Use this as the baseline before adding multi-step tools, validation, retrieval, LangGraph, or MCP.

